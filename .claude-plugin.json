{
  "name": "mcp-vision-server",
  "description": "OpenAI-compatible vision MCP server with 14 provider presets. Supports URL/local file/base64 image input. | 14 个供应商预设的 OpenAI 兼容 vision MCP 服务",
  "version": "0.1.0",
  "mcpServers": {
    "vision": {
      "command": "uvx",
      "args": ["RuyimgBy-mcp-vision-server"],
      "env": {
        "VISION_PROVIDER": "zhipu",
        "VISION_API_KEY": "${VISION_API_KEY}"
      }
    }
  }
}